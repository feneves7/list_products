import { StyleSheet } from 'react-native';


export default styles = StyleSheet.create({
    container: {
        padding: 10,
        backgroundColor: "#FFF",
        borderWidth: 1,
        borderColor: "#DDD",
        borderRadius: 5,
        padding: 20,
        marginBottom: 20,
        justifyContent:'center'
    },

    titles: {
        fontSize: 30,
        fontWeight: 'bold',
        marginBottom:15,
        marginTop:10
    },
 
    image: {
        width: 'auto',
        height: 100,
        borderColor: '#000',
        resizeMode: 'contain'
    }
});